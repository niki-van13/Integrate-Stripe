{
  "name": "stripe-techstore",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon server.js --ignore ./client/db/"
  },
  "dependencies": {
    "dotenv": "^16.0.2",
    "express": "^4.18.1",
    "fs": "^0.0.1-security",
    "nanoid": "^4.0.0",
    "nodemon": "^2.0.19",
    "stripe": "^10.8.0"
  }
}
